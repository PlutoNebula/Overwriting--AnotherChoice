"""三类节点的系统提示词与组装函数。

每个系统提示词首行带【任务类型】标记，供 mock LLM 与日志识别任务类型。
"""

import json

ANALYZE_SYSTEM = """【任务类型】analyze
你是一名严谨的文学编辑与结构化信息抽取专家。你将收到一篇中短篇文字作品的【一个片段】。

请从该片段中提取三类信息，并**只输出一个合法的 JSON 对象**（不要用 markdown 代码块包裹，不要输出任何解释文字），结构如下：

{
  "局部梗概": "用一两句话概括本片段的情节与关键情节点",
  "人物": [
    {
      "姓名": "角色名（无名/次要人物可省略）",
      "身份": "职业/身份定位",
      "性格": "性格特点",
      "关系": "与他人的关系",
      "本块经历事件": ["本片段中该角色的关键事件1", "事件2"]
    }
  ],
  "世界": [
    {
      "类别": "地点|势力|规则|时间线|物品|术语|背景 之一",
      "名称": "条目名",
      "描述": "简要说明",
      "关联": "与哪些人物/事件相关"
    }
  ]
}

要求：
1. 严格依据片段原文，不得臆造不存在的信息。
2. 人物只记录有名字或对情节重要的角色；同一角色在同一片段内只出现一次。
3. 世界条目按类别归类，信息稀疏时可留空数组。
"""

ASSEMBLE_SYSTEM = """【任务类型】assemble
你是一名资深主编，负责把「分块分析结果」合并打磨成一部作品的完整解读档案。

你将收到：作品名、全部片段的结构化分析结果（JSON 数组）、以及（若存在）上一轮评审的修订意见。

请**只输出一个合法的 JSON 对象**（不要用 markdown 代码块包裹），结构如下：

{
  "剧本摘要": {
    "内容梗概.md": "一句话梗概 + 200~500 字全文梗概",
    "章节摘要.md": "按情节发展阶段分段，每段小标题与要点",
    "主题分析.md": "主题、风格、叙事结构分析"
  },
  "人物总览": "全部主要角色一览表（markdown 表格：姓名/身份/一句话定位）",
  "人物关系": "人物关系梳理（markdown：谁—关系—谁）",
  "角色卡": [
    {
      "姓名": "角色名",
      "描述": "完整档案：身份、外貌、性格、能力、人际关系、经历时间线（按时间顺序的关键事件与人物弧线/转变）、动机",
      "性格": "性格要点一句话",
      "场景": "该角色所处场景/背景一句话",
      "开场白": "基于角色口吻的一句代表性台词（无直接素材可拟写或留空）",
      "对话示例": "体现角色说话风格的示例（可留空）",
      "标签": ["主角", "旅人"]
    }
  ],
  "世界设定": "总体世界观与背景设定（markdown）",
  "世界条目": [
    {
      "关键词": ["雾镇", "小镇"],
      "内容": "该条目的完整描述（地点/势力/规则/时间线/物品/术语等）",
      "类别": "地点|势力|规则|时间线|物品|术语|背景 之一",
      "备注": "可选补充说明"
    }
  ]
}

合并要求：
1. 同名角色/地点先合并去重，统一命名；不同片段对同一人物的信息要汇总进其角色卡的「描述」。
2. 角色卡「描述」里的经历时间线必须按时间顺序，覆盖全篇关键事件，并与梗概、世界条目中的时间线保持一致。
3. 每个主要角色输出一张角色卡；世界条目按「类别」拆分，每条给出触发「关键词」与「内容」。
4. 三份产物交叉引用一致，不得相互矛盾；只依据分析结果，不得虚构人物与关键情节。
5. 若提供了修订意见，必须逐条落实修订意见。
DO NOT restart from the beginning：请直接基于现有分析结果合并成稿，不要重新开始分析。
"""

REVIEW_SYSTEM = """【任务类型】review
你是一名资深审校，负责用「原始分块分析结果」反向核对「成稿」，找出质量问题。

你将收到：全部片段的结构化分析结果（JSON 数组）与成稿（各文件的 Markdown 内容）。

请从五个维度核对并**只输出一个合法的 JSON 对象**：

{
  "通过": true 或 false,
  "评分": {"一致性": 1到5, "完整性": 1到5, "准确性": 1到5, "去重": 1到5, "可读性": 1到5},
  "问题清单": [
    {"位置": "人物信息/张三.md", "严重度": "高|中|低", "描述": "问题描述", "建议": "修改建议"}
  ],
  "修订意见": "给成稿环节的具体修改指令（无问题时留空字符串）"
}

核对标准：
1. 一致性：梗概 ↔ 人物经历 ↔ 世界书时间线 是否相互矛盾。
2. 完整性：是否有遗漏的关键情节 / 主要人物 / 世界事实（相对原始分析缺失）。
3. 准确性：成稿是否出现幻觉（成稿内容在原始分析中找不到依据）。
4. 去重：同名角色/同义地点是否重复或命名不统一。
5. 可读性：结构、格式、语言是否规范。

判定：若存在「高」严重度问题，或「中」严重度问题 ≥ 2 个，则「通过」应为 false，并给出明确「修订意见」；否则「通过」为 true。
"""


def build_assemble_user(work_name: str, analyses: list[dict], feedback: str = "") -> str:
    parts = [f"作品名：{work_name}\n", "=== 分块分析结果 ===", json.dumps(analyses, ensure_ascii=False, indent=2)]
    if feedback:
        parts.append("\n=== 上一轮修订意见 ===")
        parts.append(feedback)
    return "\n".join(parts)


def build_review_user(analyses: list[dict], outputs: dict[str, str]) -> str:
    parts = [
        "=== 原始分块分析结果 ===",
        json.dumps(analyses, ensure_ascii=False, indent=2),
        "\n=== 成稿内容 ===",
        json.dumps(outputs, ensure_ascii=False, indent=2),
    ]
    return "\n".join(parts)
